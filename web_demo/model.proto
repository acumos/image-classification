syntax = "proto3";
package MkQqqYoJoeivdiNvJPVSnhUlmFzKVten;

service Model {
  rpc classify (ImageSet) returns (ImageTagSet);
}

message ImageTagSet {
  repeated int64 image = 1;
  repeated string tag = 2;
  repeated double score = 3;
}

message ImageSet {
  repeated string mime_type = 1;
  repeated bytes image_binary = 2;
}